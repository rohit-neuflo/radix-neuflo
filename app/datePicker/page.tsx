"use client";
import React from "react";
import { Calendar } from "@/src"; // Assuming the DatePicker component is in a separate file

export default function App() {
  return (
    <div>
      <Calendar range />
    </div>
  );
}
